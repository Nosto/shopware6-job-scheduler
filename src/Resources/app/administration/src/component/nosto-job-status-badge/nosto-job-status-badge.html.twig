<span class="nosto-job-status-badge"
      :class="classes"
      :aria-label="label"
      :title="label">
    <span class="nosto-job-status-dot" aria-hidden="true"></span>
    <span v-if="showLabel">{{ label }}</span>
</span>
